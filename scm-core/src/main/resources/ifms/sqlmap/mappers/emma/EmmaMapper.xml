<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="ifms.emma.mapper.EmmaMapper">
	<insert id="insertEmSmtTran" parameterType="map" useGeneratedKeys="true"
        keyProperty="mtPr"
        keyColumn="mt_pr">
	    INSERT INTO emma.em_smt_tran (
	        date_client_req,
	        content,
	        callback,
	        service_type,
	        msg_status,
	        recipient_num
	    ) VALUES (
	        NOW(),
	        #{content},
	        #{callback},
	        '0',
	        '1',
	        #{receipientNum}
	    )
	</insert>
</mapper>